let svg = d3.select("body").append("svg")
  .attr("width", 250)
  .attr("height", 250);

let rect = svg.append("rect")
    .attr("x", 50)
    .attr("y", 50)
    .attr("width", 20)
    .attr("height", 20)
